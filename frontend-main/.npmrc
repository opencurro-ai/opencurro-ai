# TypeScript 7 (latest stable) is newer than some tools' declared peer ranges
# (e.g. typescript-eslint still advertises <6.1). The tooling works correctly at
# runtime, so we accept the stale peer metadata instead of downgrading TypeScript.
legacy-peer-deps=true
