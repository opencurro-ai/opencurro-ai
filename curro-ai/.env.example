# Curro AI backend configuration

# Port the Express server listens on
PORT=8787

# Absolute or relative path to the agent workspace.
# Defaults to <curro-ai>/workspace when unset. All file tools are sandboxed to this directory.
WORKSPACE_ROOT=

# Maximum ReAct iterations per turn
MAX_ITERATIONS=1000

# Comma separated list of allowed CORS origins, or * for all
CORS_ORIGINS=*

# Timeout (ms) for shell commands run via the shall_tool
SHELL_TIMEOUT_MS=180000
